import * as React from 'react';
import { ReactElement } from 'react';
interface ExpandTextProps {
    maxHeight: number;
    children?: ReactElement | ReactElement[] | string | number;
}
export declare const ExpandText: React.FC<ExpandTextProps>;
export {};
