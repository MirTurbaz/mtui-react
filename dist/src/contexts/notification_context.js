import { createContext } from 'react';
export const NotificationContext = createContext({
    addNotification: (notification) => '',
    deleteNotification: (id) => { },
});
//# sourceMappingURL=notification_context.js.map