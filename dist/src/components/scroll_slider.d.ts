import * as React from 'react';
import { ReactElement } from 'react';
export interface ScrollSliderProps {
    className?: string;
    children?: ReactElement;
    scrollWidth?: number;
}
export declare const ScrollSlider: React.FC<ScrollSliderProps>;
